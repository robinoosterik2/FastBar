<div class="mb-4">
  <%= form.label field, label_text || field.to_s.humanize, class: "block text-gray-700 mb-2 font-medium" %>
  <%= form.send(input_type || :text_field, field, class: "w-full px-4 py-2 rounded border border-gray-300 focus:ring-primary focus:border-primary transition", **(options || {})) %>
</div>
