<div class="w-full lg:w-2/3 flex flex-col mx-auto">
  <%= render Home::HeaderComponent.new(active_tab: @active_tab) %>
  <main class="container mx-auto mt-12 px-5 flex flex-col items-center min-h-screen">
    <%= pageContent %>
  </main>
</div>